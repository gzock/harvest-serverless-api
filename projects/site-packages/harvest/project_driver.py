import os, sys
import json

import boto3
from boto3.dynamodb.conditions import Key, Attr

from harvest.dynamodb_driver import DynamoDbDriver

class ProjectDriver():

  def __init__(self, host, port):
    self.dynamodb = DynamoDbDriver(host, port, "Projects")
  
  def get_all(self):
    return self.dynamodb.scan()


