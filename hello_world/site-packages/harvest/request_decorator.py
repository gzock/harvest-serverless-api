class RequestDecorator():

  def __init__(self, events):
    if not events:
      raise ValueError

    if "requestContext" in events 
      and "identity" in events["requestContext"]:
      self.__identity_id = events["requestContext"]["identity"]["cognitoIdentityId"]
      self.__username = events["requestContext"]["authorizer"]["claims"]["cognito:username"]
      self.__query = event["queryStringParameters"]
      self.__events = events

    else
      raise ValueError
  
  def get_identity_id(self):
    return self.__identity_id

  def get_username(self):
    return self.__username

  def get_query(self):
    return self.__query

  def get_raw_events(self):
    return self.__events
